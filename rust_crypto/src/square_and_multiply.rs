//use std::io;

//change a signed integer into a binary array
fn integer_64_to_bit(y: u64) -> [i8; 64]{

    let tab : [i8; 64] = [0; 64];
    
    return tab;
}

// expo modu (x puiss y) mod n
pub fn sam(x: u64, y: u64 , n: u64) -> u64{
   


    return 10;
}